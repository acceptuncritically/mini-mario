{
  "name": "mini-mario",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "2D 横向卷轴平台跳跃小游戏（类马里奥）— Web Canvas + 原生 JavaScript",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --configLoader=native",
    "test:watch": "vitest --configLoader=native",
    "bundle": "node scripts/build-single-file.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
